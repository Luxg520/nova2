using Swift;

namespace Nova
{
    public enum UserInfoIntDataPos
    {
        Money, // 银币
        Level, // 等级
		VipLevel,//VIP等级
        Exp, // 经验
		VipExp, //VIP经验
        Diamond, // 钻石
		BindDiamond, // 绑定钻

        Count
    }

    public enum UserInfoStrDataPos
    {
        ID,
        Name,
        PWD, 

        Count
    }

    public enum UserInfoULongDataPos
    {
        Count,
    }
	
    public enum UserInfoObjDataPos
    {
        Count,
    }

    public enum UserInfoIntLstDataPos
    {
        Count,
    }
    public enum UserInfoStrLstDataPos
    { 
        Count,
    }

    public enum UserInfoLstDataPos
    {
        Count,
    }

    public enum UserInfoULongLstDataPos
    {
        Count,
    }

    public enum UserInfoIIDictDataPos
    {
        Count,
    }

    public enum UserInfoSIDictDataPos
    { 
        Count,
    }
}
